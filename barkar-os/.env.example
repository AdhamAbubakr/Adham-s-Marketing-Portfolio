# Barkar OS — copy to .env.local and fill when the NEW Supabase project + Vercel exist.
# Blueprint §17.4 secrets management. NEVER commit .env.local.

# --- Supabase (NEW clean project for the OS — see blueprint §13 #3) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=          # server-only, never exposed to browser

# --- App ---
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- Phase 3+ (leave blank until those phases) ---
ANTHROPIC_API_KEY=
RESEND_API_KEY=
META_APP_ID=
META_APP_SECRET=
